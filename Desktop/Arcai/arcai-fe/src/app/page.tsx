import { Shell } from "@/components/layout";

export default function Home() {
  return (
    <Shell>
      <h1 className="text-2xl font-semibold">Dashboard</h1>
      <p className="text-gray-500 mt-2">Your workspace is ready.</p>
    </Shell>
  );
}